import XCTest
@testable import SelectableCollectionView

final class SelectableCollectionViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SelectableCollectionView().text, "Hello, World!")
    }
}
